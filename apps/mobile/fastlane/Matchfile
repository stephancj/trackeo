git_url(ENV["MATCH_GIT_URL"] || "https://github.com/nyandrianinamamy/ios-certs.git")
storage_mode("git")

type("appstore")

app_identifier(["com.trackeo.client.app"])
username(ENV["APPLE_ID"])
team_id(ENV["APPLE_TEAM_ID"])
